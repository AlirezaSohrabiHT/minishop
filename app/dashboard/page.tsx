import { Button } from "@/components/ui/button"
export default function Dashboard () {
    return(
        <section className="flex flex-col justify-center items-center h-[100vh]">
            <Button className="bg-amber-300 text-black">Click</Button>
        </section>
    )
}